{
  "name": "ghas-policy-as-code",
  "version": "1.0.0",
  "description": "A GitHub Action to check for secret scanning alerts and block PRs if necessary.",
  "main": "src/main.ts",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "jest": "^26.6.3",
    "typescript": "^4.3.5"
  }
}
